{
  "name": "inboxd",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "platforms/*",
    "contrib/*"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.base.json --noEmit",
    "lint": "bun run typecheck",
    "check:boundaries": "bun scripts/check-boundaries.ts",
    "test": "NODE_ENV=test SQLCIPHER_PATH=\"$(brew --prefix sqlcipher)/lib/libsqlcipher.dylib\" bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
